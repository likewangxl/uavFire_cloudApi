# 无人机火情智巡微信小程序设计文档

> 文档版本：v1.1
>
> 编制日期：2026-09-03
>
> 适用分支：`feature/m300-model-adaptation`
>
> 建议代码目录：`miniprogram/`
>
> 当前状态：Phase 1 开发基线已启动；后端 BFF 与原生小程序壳已落地，尚未完成微信联调、设备联调或真机验收

## 1. 文档导航

| 文档 | 内容 | 主要读者 |
| --- | --- | --- |
| [01-产品与业务设计](01-product-design.md) | 产品定位、角色、范围、信息架构、业务流程、状态机、页面与验收口径 | 领导、产品、项目经理、研发、测试 |
| [02-详细技术方案](02-technical-architecture.md) | 总体架构、模块划分、数据模型、消息与报告、直播、安全、部署和可观测性 | 架构、后端、小程序、运维、安全 |
| [03-API 接口文档](03-api-contract.md) | API 规范、认证、错误码、接口清单、请求响应示例、实时事件契约 | 小程序、后端、测试、第三方联调 |
| [openapi.yaml](openapi.yaml) | OpenAPI 3.0 可机读契约，可导入 Swagger Editor、Apifox、Postman | 前后端、测试 |
| [04-实施计划与验收](04-implementation-plan.md) | 工作分解、依赖、里程碑、测试矩阵、发布回滚、风险与验收门禁 | 项目经理、研发、测试、运维 |
| [05-Phase 1 开发记录](05-phase1-foundation.md) | 已实现基座、配置开关、验证证据、待办和联调输入 | 产品、研发、测试、运维 |
| [06-多机型兼容与验收](06-multi-aircraft-compatibility.md) | M3 行业系列、M30、M300/M350、M3D、M4、M400 和后续机型的能力分层、适配器与逐组合验收 | 架构、Agent、后端、测试、飞行、安全 |

## 2. 核心结论

小程序定位为“领导移动驾驶舱”，不是遥控器替代品。首版围绕以下闭环建设：

```text
微信登录与组织绑定
  -> 今日态势与待办
  -> 巡检任务和实时进度
  -> AI 异常/火情确认
  -> 审批、批示、派单和督办
  -> 自动生成巡检报告
  -> 微信订阅消息送达
  -> 复检、签阅和归档
```

复杂航线规划继续由 PC 端承担。小程序只浏览已发布航线、完成审批，并对已通过安全预检的任务发起受控操作。

当前工作分支来自 M300 适配线，但产品范围不是 M300 专用。系统按机型族、控制端、
载荷和运行时能力选择适配器；未知型号可只读展示真实遥测，但任何控制默认关闭。

## 3. 现有能力与新增范围

### 3.1 直接复用

- `planned_wayline` 航线计划、KMZ 生成、发布、准备和任务状态。
- RC Plus Agent 的航线下载、执行、暂停、恢复、停止、进度和事件回传。
- MSDK 设备状态、OSD/HMS、可见光直播和 ZLMediaKit 播放链路。
- 火情事件、AI 识别证据、坐标质量、空间合并、事件处置和任务编排。
- PC 领导驾驶舱已有的指标定义、地图态势和在线设备口径。
- FC100 合规留证、资源分配、任务状态机和派发前预检。

### 3.2 新增能力

- 微信身份登录、系统账号绑定和组织权限映射。
- 面向移动端的聚合 API（Mini Program BFF）。
- 领导审批、批示、督办、复检和签阅闭环。
- 通用巡检问题单和整改状态机。
- 巡检报告模板、异步生成、版本、签阅和归档。
- 微信订阅消息授权、发送、失败重试和送达审计。
- 持久化飞行指令队列、幂等、二次确认、回执和超时闭环。
- 统一审计日志、移动端风险控制和可观测指标。

### 3.3 上线前置项

1. 将 `WaylineAgentServiceImpl.pendingByDrone` 的内存单槽命令升级为持久化队列，禁止新命令静默覆盖未完成命令。
2. 建立逐组合兼容矩阵；对拟开放的每个飞机、控制端、负载、固件和 Agent/Cloud 适配器组合完成真机、实载荷和实飞验收。
3. 保持所有未完成专项验收的火情闭环和高风险能力默认关闭，按机型与动作受控灰度。
4. 配置可从微信公网访问的 HTTPS/WSS 域名、证书、API 网关和媒体出口；不得暴露内网管理接口。
5. 完成微信小程序主体、服务类目、订阅消息模板和 `live-player` 权限审核。
6. 高风险操作必须完成 RBAC、二次确认、幂等、操作原因、审计和飞手现场确认。
7. `MINIAPP_FLIGHT_CONTROL_ENABLED=true` 不能单独放行；请求还必须命中已签字的精确组合键和运行时能力快照。

## 4. 事实源与边界

- 当前生产能力和现场证据以 `../CURRENT_PROJECT_STATUS_2026-08-05.md`、仓库根目录 `RUNBOOK.md`、当前代码及专项验收记录为准。
- 本文档描述目标方案，不代表对应代码已经实现、部署或通过设备/飞行验收。
- 小程序不得绕过 DJI Pilot 2、飞手、官方安全限制或正式空域审批系统。
- AI 只提供发现、风险排序和辅助摘要，不自动批准起飞、投放、返航或归档真实事件。

## 5. 版本管理

- 文档、OpenAPI 和实现代码必须使用相同版本号。
- API 破坏性变化通过 `/miniapp/api/v2` 发布，不在 v1 原地改变语义。
- 文档修改必须同步维护 `openapi.yaml`、错误码表、状态机和验收用例。
- 每次现场验收单独形成记录，明确设备 SN、机型、载荷、Agent/APK、后端提交、测试环境和结论。

## 6. Phase 1 已落地范围（2026-09-03）

- 新增 `/miniapp/api/v1` 独立 Bearer 鉴权、统一响应、请求追踪和错误处理。
- 新增 `/auth/wechat/login`、`/me`、`/dashboard/summary` 三个首批接口。
- 新增可导入微信开发者工具的 `miniprogram/` 工程及首页、任务、报告、系统页面。
- 所有开关默认关闭；微信登录当前按设计返回 `WECHAT_API_UNAVAILABLE`，直到服务端适配器和账号绑定完成。
- 首页仅在明确启用且部署满足工作空间隔离条件时读取实时 MSDK 在线数量；其余指标明确标注为部分数据，不返回伪造值。
