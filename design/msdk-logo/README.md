# MSDK 标识概念稿

`concept-a.svg`、`concept-b.svg`、`concept-c.svg` 为本地保存的历史矢量概念稿，2026-09-05 补入版本管理。SVG 为可编辑源文件；同名 PNG 预览可由 SVG 重新渲染，不随本次提交收录。

当前 Agent 启动图标以 `rcplus-msdk-agent/app/src/main/res/` 为准，这三份概念稿不参与 Android 构建。
