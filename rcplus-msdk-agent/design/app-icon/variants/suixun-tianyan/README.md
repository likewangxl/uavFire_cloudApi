# “燧巡天眼”标识方案归档

2026-09-05 将旧根工作区尚未提交的标识修改保存为独立设计方案。当前 `main` 继续使用“火情智巡平台”及其现有图标，本目录不参与 Android 构建。

`source/` 按 Agent 模块原有路径保存 19 个文件的完整快照：15 张各密度启动图标、1 张设计母图，以及 Manifest、应用名称、图标背景色 3 个 XML 文件。该方案应用名称为“燧巡天眼”，背景色为 `#031A4D`。

这些文件来自旧分支基线 `b47042b9f62ea1e1d82ba85baa9d2efe48fdb42a` 的未提交工作。`branding-text-changes.patch` 记录其中 3 个 XML 相对该基线的修改；图片本身保存在 `source/`。逐文件来源和 SHA-256 见仓库内 `docs/local-source-archive-2026-09-05.json`。

如后续启用此方案，只将所需名称、颜色和图片整合进当前资源目录，并保留主线新增的权限、Activity 和其他字符串。不要用这里的旧 Manifest 或 strings.xml 整文件覆盖主线。完成后需验证资源绑定与图标尺寸；本次归档没有打包或安装 APK。
