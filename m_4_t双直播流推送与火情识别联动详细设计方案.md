# M4T双直播流推送与火情识别联动详细设计方案

## 1. 文档说明

### 1.1 文档目的

本文档用于设计“基于 M4T 双直播流推送”的专项方案，重点覆盖以下内容：

1. M4T 是否具备双流推送的实现前提。
2. 在接入 MSDK 的情况下，如何实现可见光流与红外流的并行获取、并行推送和并行消费。
3. 如何将双流能力应用到驾驶舱直播、火情识别、红外复核和后续灭火处置流程中。
4. 如何在现有 Cloud API + MSDK + ZLMediaKit + AI 服务架构基础上，扩展为双流直播中枢方案。

本方案是针对“直播与视频流专项”的详细设计，不替代此前的全流程灭火联动总方案，而是作为其子方案和媒体能力专项设计文档。

---

## 2. 设计结论

## 2.1 总体结论

接入 MSDK 后，系统具备向“双流并行”演进的技术基础，但不能简单理解为“接入 MSDK 就天然自动支持双直播流推送”。

### 2.1.1 能明确确认的能力

MSDK 的新版图传能力支持：
- 获取可用相机索引
- 指定相机索引显示画面
- 指定相机索引接收原始码流
- 指定相机索引接收帧数据
- 支持同一相机画面输出到多个 Surface
- 支持通过 KeyCameraVideoStreamSource 进行镜头切换

这说明 MSDK 在能力模型上，已经具备“按相机索引 / 视频源分别获取数据”的基础。

### 2.1.2 不能直接拍板保证的部分

官方文档明确支持：
- 多 Surface 显示
- 指定相机索引取码流
- 指定相机索引取帧

但我没有查到足够直接的官方表述，能够替你保证：

> 在 M4T + RC Plus 2 的具体组合上，一定可以无条件同时稳定输出两路独立直播推流（例如一路可见光、一路红外）并长期运行。

因此，本方案的设计前提是：

### 结论性判断

1. **架构上可以按双流并行设计。**
2. **实现上必须先做 PoC 验证，确认 M4T 在你的固件、遥控器、MSDK 版本组合下，能否同时稳定提供两路所需视频源。**
3. **如果 PoC 通过，本方案可以直接落地。**
4. **如果 PoC 不通过，则退化为“单流 + 快速切镜头 + 逻辑双流”方案。**

---

## 3. 适用场景

本方案适用于以下场景：

1. 驾驶舱页面需要同时显示可见光与红外热成像画面。
2. AI 服务需要同时消费可见光流和红外流，执行融合火情识别。
3. 火情发现后，不希望通过“切镜头”中断当前主画面，而是希望两路画面始终并行存在。
4. 后续需要支持：
   - 主副画面切换
   - 双流录像
   - 双流截图
   - 双流事件留痕
   - 双流与 FC100 灭火处置联动

---

## 4. 技术架构设计

## 4.1 总体架构

```text
网页端指挥平台
    ↓
业务后端 / 调度中枢
    ├─ Cloud API 接入层
    │    ├─ 任务下发
    │    ├─ 状态接收
    │    └─ 云端任务管理
    │
    ├─ 遥控器侧 MSDK 双流执行器
    │    ├─ 可见光流获取
    │    ├─ 红外流获取
    │    ├─ 双路码流监听
    │    ├─ 双路帧监听
    │    ├─ 云台/镜头控制
    │    └─ 双流推送控制
    │
    ├─ 流媒体服务（ZLMediaKit）
    │    ├─ visible 主流接入
    │    ├─ thermal 主流接入
    │    ├─ WebRTC 转发
    │    ├─ HTTP-FLV 转发
    │    └─ AI 拉流入口
    │
    ├─ AI 分析服务
    │    ├─ 可见光火焰/烟雾识别
    │    ├─ 红外热点识别
    │    ├─ 双流时空融合
    │    └─ 风险评分
    │
    └─ 火情事件中心
         ├─ 双流证据归档
         ├─ 事件生成
         ├─ 任务派发
         └─ FC100 处置联动
```

---

## 4.2 设计原则

### 4.2.1 Cloud API 仍然负责任务总控
Cloud API 负责：
- 巡航航线下发
- 飞行任务状态管理
- 设备接入与远程任务编排
- 后续与 FC100 的灭火任务协同

### 4.2.2 MSDK 负责双流近端能力
MSDK 负责：
- 获取双路视频源
- 双路推流
- 双路帧级数据供 AI 使用
- 相机与云台控制
- 近端执行逻辑

### 4.2.3 ZLMediaKit 作为双流媒体中枢
ZLMediaKit 负责：
- 接收双路 RTMP/RTSP
- 转 WebRTC / HTTP-FLV 给前端
- 提供 AI 服务统一拉流地址
- 可选录制与转协议

---

## 5. 双流能力实现分析

## 5.1 MSDK 能力基础

MSDK 新版图传能力支持：
- addAvailableCameraUpdatedListener：监听可用相机索引
- addReceiveStreamListener：接收指定相机索引的原始码流
- addFrameListener：接收指定相机索引、指定格式的帧数据
- putCameraStreamSurface：将相机画面输出到指定 Surface
- KeyCameraVideoStreamSource：镜头视频源切换

从能力模型看，MSDK 已经不是传统“只有一个固定视频通道”的模型，而是“按相机索引和需要的数据类型获取数据”的模型。

### 关键推断

这意味着你有机会在遥控器侧：
- 同时监听两个视频源
- 分别获取可见光与红外数据
- 各自进行推流和分析

但前提是：设备本身在当前模式下确实对外暴露并支持这两个源的并行取数。

---

## 5.2 双流实现前提

要实现双直播流推送，必须满足以下条件：

### 5.2.1 设备层前提
1. M4T 在当前模式下能够同时提供可见光与红外对应的视频源或相机索引。
2. RC Plus 2 上的 MSDK App 能同时订阅两路数据。
3. 两路数据不会因为硬件编码器资源限制而互相挤占到不可用。

### 5.2.2 软件层前提
1. MSDK App 需实现双 listener 管理。
2. 本地推流模块需支持双实例推流。
3. 流媒体服务需支持双流命名和统一管理。
4. 前端需支持双播放器或主副布局。
5. AI 服务需支持双流并发分析与融合。

---

## 5.3 PoC 验证建议

在正式立项双流前，必须先完成 PoC 验证。

### 5.3.1 验证目标
1. 是否能同时拿到可见光和红外两个视频源。
2. 是否能同时输出两路码流。
3. 是否能同时输出两路帧数据。
4. 连续运行 30 分钟以上是否稳定。
5. CPU、GPU、遥控器温度、延迟、丢帧是否可接受。

### 5.3.2 PoC 结论判定
- **通过**：进入双流正式方案。
- **不通过**：退化为“单流 + 镜头切换 + 逻辑双流模拟”方案。

---

## 6. 双流媒体设计

## 6.1 流命名规范

建议采用“流组 + 子流类型”的方式，而不是把每一路当独立业务对象。

### 6.1.1 流组定义
- stream_group_id = `{workspace_id}_{drone_sn}`

### 6.1.2 子流定义
- visible_stream_id = `{stream_group_id}_visible`
- thermal_stream_id = `{stream_group_id}_thermal`

示例：
- `ws001_m4t001_visible`
- `ws001_m4t001_thermal`

---

## 6.2 推流协议设计

### 6.2.1 遥控器侧到流媒体服务
建议采用 RTMP 推流：
- 实现成熟
- ZLMediaKit 接入方便
- 适合双实例推送

示例：
- `rtmp://media/live/ws001_m4t001_visible`
- `rtmp://media/live/ws001_m4t001_thermal`

### 6.2.2 流媒体服务到前端
建议：
- 主播放：WebRTC
- 备选：HTTP-FLV

### 6.2.3 流媒体服务到 AI 服务
建议：
- RTSP 或 RTMP 拉流

---

## 6.3 双流播放设计

### 6.3.1 驾驶舱布局方式
支持两种布局：

#### 布局 A：主副画面
- 主画面：可见光
- 小窗：红外
- 支持主副互换

#### 布局 B：左右分屏
- 左侧：可见光
- 右侧：红外

### 6.3.2 切换逻辑
前端的“切红外复核”在双流模式下不再是“切换镜头”，而是：
- 高亮红外窗口
- 放大红外主画面
- AI 提升红外权重

即：
**双流模式下的复核，是“切换关注焦点”，不是“切换唯一视频源”。**

---

## 7. 双流 AI 识别设计

## 7.1 设计目标

利用双流同时存在的优势，提高火情识别准确率，降低误报率。

---

## 7.2 可见光分析子模块

职责：
- 识别火焰
- 识别烟雾
- 连续帧稳定性检测
- 目标跟踪

输出：
- fire 候选
- smoke 候选
- 可见光置信度
- 目标框

---

## 7.3 红外分析子模块

职责：
- 提取异常热点
- 热斑持续性判断
- 高热区域面积变化判断
- 热异常阈值判断

输出：
- thermal hotspot 候选
- 热异常分值
- 热点框

---

## 7.4 双流融合判定模块

### 7.4.1 融合逻辑
在时间和空间上对齐两路结果：
- 时间窗口对齐
- 空间位置近邻匹配
- 风险评分加权

### 7.4.2 典型判定策略
#### 低风险
- 可见光命中，但红外无明显热点

#### 中风险
- 可见光和红外任一侧连续命中，但另一侧较弱

#### 高风险
- 可见光火焰/烟雾连续命中
- 红外存在持续热点
- 两路目标在同区域稳定重合

---

## 8. 业务流程设计

## 8.1 巡航巡检流程

```text
Cloud API 下发巡航任务
→ M4T 执行巡航
→ MSDK 同时获取 visible / thermal 两路流
→ 推送到 ZLMediaKit
→ 前端驾驶舱双画面显示
→ AI 服务同时拉取两路流执行分析
→ 生成疑似火点事件
```

---

## 8.2 火情复核流程

```text
AI 在 visible 中发现疑似火点
→ 自动关联 thermal 同时刻区域
→ 若 thermal 也存在异常热点
→ 直接进入高置信复核通过
→ 事件中心生成 CONFIRMED 事件
→ 输出火点坐标
```

双流模式下，这个复核流程不依赖“现场切镜头”。

---

## 8.3 处置派发流程

```text
CONFIRMED 火情事件生成
→ 事件中心调用调度规则
→ 派发 FC100 处置任务
→ FC100 飞往目标区域
→ 执行灭火
→ 作业结果回传
```

---

## 9. 前端设计

## 9.1 页面结构

### 9.1.1 驾驶舱页
- 地图区域
- 双直播区
- 当前任务状态区
- 火情告警区
- 处置任务区

### 9.1.2 双直播区功能
- 可见光播放窗口
- 红外播放窗口
- 主副互换按钮
- 单独全屏按钮
- 当前流状态展示
- 同步截图按钮

---

## 9.2 交互设计

### 9.2.1 火情告警交互
当 visible 检出疑似火点时：
- 可见光窗口高亮目标框
- 红外窗口自动定位对应区域并高亮
- 告警面板提示“双流复核中”

### 9.2.2 复核通过交互
- 双窗口均显示对应标注
- 告警升级
- 弹出“派发 FC100”按钮

---

## 10. 后端设计

## 10.1 模块划分

### 10.1.1 双流控制模块
负责：
- 查询流组信息
- 获取双流播放地址
- 查询当前推流状态
- 触发双流启停

### 10.1.2 双流任务管理模块
负责：
- 绑定某架 M4T 与双流组
- 管理 visible / thermal 子流状态
- 维护双流健康检查

### 10.1.3 AI 融合服务模块
负责：
- 接收双流识别结果
- 做时空融合
- 输出风险分值

---

## 10.2 接口设计

## 10.2.1 查询双流信息
**GET** `/api/v1/live/groups/{drone_sn}`

返回示例：
```json
{
  "stream_group_id": "ws001_m4t001",
  "visible": {
    "stream_id": "ws001_m4t001_visible",
    "play_url_webrtc": "webrtc://media/live/ws001_m4t001_visible",
    "play_url_flv": "https://media/live/ws001_m4t001_visible.live.flv",
    "status": "ONLINE"
  },
  "thermal": {
    "stream_id": "ws001_m4t001_thermal",
    "play_url_webrtc": "webrtc://media/live/ws001_m4t001_thermal",
    "play_url_flv": "https://media/live/ws001_m4t001_thermal.live.flv",
    "status": "ONLINE"
  }
}
```

### 10.2.2 启动双流推送
**POST** `/api/v1/live/groups/{drone_sn}/start`

### 10.2.3 停止双流推送
**POST** `/api/v1/live/groups/{drone_sn}/stop`

### 10.2.4 主副画面切换记录
**POST** `/api/v1/live/groups/{drone_sn}/focus`

请求示例：
```json
{
  "focus_stream": "thermal"
}
```

---

## 11. 数据库设计

## 11.1 双流组表 live_stream_group

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| stream_group_id | varchar(64) | 流组编号 |
| workspace_id | varchar(64) | 工作空间编号 |
| drone_sn | varchar(64) | 无人机编号 |
| status | varchar(32) | 流组状态 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

## 11.2 双流明细表 live_stream_item

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| stream_group_id | varchar(64) | 流组编号 |
| stream_type | varchar(32) | VISIBLE / THERMAL |
| stream_id | varchar(64) | 流编号 |
| push_url | varchar(512) | 推流地址 |
| play_url_webrtc | varchar(512) | WebRTC 播放地址 |
| play_url_flv | varchar(512) | FLV 播放地址 |
| status | varchar(32) | 状态 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

## 11.3 双流证据表 fire_dual_stream_record

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| event_id | varchar(64) | 火情事件编号 |
| visible_screenshot_url | varchar(512) | 可见光截图 |
| thermal_screenshot_url | varchar(512) | 红外截图 |
| visible_bbox | text | 可见光框 |
| thermal_bbox | text | 红外框 |
| fusion_score | decimal(5,4) | 融合分值 |
| create_time | datetime | 创建时间 |

---

## 12. 关键风险与应对

## 12.1 设备双流能力不达预期
### 风险
实际设备可能无法稳定双路并行输出。

### 应对
先做 PoC；若不通过，退化为单流方案。

## 12.2 遥控器算力或编码压力过大
### 风险
双路监听和双路推流可能造成高负载。

### 应对
限制码率、分辨率和帧率；必要时一路用于直播，一路只取帧不推流。

## 12.3 双流时间同步误差
### 风险
可见光和红外不同步会影响融合识别。

### 应对
在 AI 服务中引入时间窗口匹配和缓冲队列。

## 12.4 前端体验复杂度增加
### 风险
双播放器导致页面复杂。

### 应对
采用主副画面模式，提供一键互换。

---

## 13. 分期实施建议

## 13.1 第一阶段：PoC 验证
- 遥控器侧实现双源监听 Demo
- 验证 visible / thermal 是否能同时获取
- 验证双路推流到 ZLMediaKit
- 验证前端双窗口播放

## 13.2 第二阶段：双流直播版
- 双流组管理
- 驾驶舱双播放
- 双流启停控制
- 主副画面切换

## 13.3 第三阶段：双流识别版
- 可见光检测
- 红外检测
- 双流融合评分
- 事件中心联动

## 13.4 第四阶段：灭火联动版
- 火情确认后自动派发 FC100
- 处置结果回流
- 双流证据归档

---

## 14. 最终建议

1. **可以按双流并行方案设计，但必须先做 PoC。**
2. **不要把“接入 MSDK”直接等同于“无条件支持双直播流推送”。**
3. **一旦 PoC 通过，双流方案将明显优于单流切镜头方案。**
4. **对火情识别而言，双流方案的价值非常大，因为它消除了复核时的画面切换中断。**
5. **双流专项方案应作为总灭火系统中的媒体与识别子系统实施，不应脱离 Cloud API 和 FC100 处置体系单独建设。**

---

## 15. 一句话结论

在接入 MSDK 后，你的系统具备实现“双流并行”的技术基础，但这不是一个可以直接口头保证的现成功能，而是一个“官方能力支持方向明确、落地前必须做真机 PoC 验证”的方案。若验证通过，建议采用“可见光 + 红外双流并行、ZLMediaKit 中枢转发、AI 双流融合识别、事件联动 FC100”的完整双流设计路线。

