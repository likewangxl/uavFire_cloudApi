# 驾驶舱原型验收记录

日期：2026-09-05。范围：用户要求的五页信息架构与前端交互演示。

## 比较依据

- 原界面参考：`../../codex-left-panel-preview-wide.png`（文件实际为 JPEG，1888×855）。
- 产品目标：将原界面八个指标与常驻左右信息栏拆分为五个页面，首页只保留三项可操作计数、地图、待办与任务摘要。
- 实现：`http://127.0.0.1:5188/`。
- 最终桌面截图：`qa/01-overview.jpg`、`qa/02-video.jpg`、`qa/03-events.jpg`、`qa/04-tasks.jpg`、`qa/05-devices.jpg`、`qa/08-twenty-feeds.jpg`。
- 桌面 CSS 视口：1280×720，浏览器 DPR 2；截图工具输出可见内容 1274×717 JPEG，已由工具归一为接近 CSS 尺寸，无额外缩放处理。
- 窄屏 CSS 视口：390×844；`qa/06-mobile-overview.jpg`、`qa/07-mobile-events.jpg` 为 384×831 可见内容捕获。
- 曾尝试按原参考 1888×855 设置视口，但工具截图裁切为 1742×852，该截图已舍弃；最终按实际窗口复查，不把工具裁切认作页面溢出。
- 旧参考与新原型的内容、视口和状态不同：旧图是既有业务页，新图是明确标记的示例数据。本次是用户授权重组信息架构后的产品质量比较，不声称逐像素还原旧页。

## 全图与重点区域证据

原参考和最终 `01-overview.jpg` 在同一图片比较输入中打开。可见原来的八项指标缩为三个可点击事项，移除了两侧常驻大信息栏、全局装饰性状态和工程信息；深色底、蓝色交互和事件暖色提示保留。

视频、事件、任务、设备的实际浏览器截图逐一检查。重点区域使用 1280 像素宽完整可读截图中的导航、视频标签、事件列表、设备表格与复核弹窗；移动端单独捕获导航与事件卡片，无需另做图片裁切。长列表、处理记录等通过实际滚动及按钮操作检查，不把首屏截断当作全页截图。

## 五项视觉检查

| 表面 | 结论 |
| --- | --- |
| 字体 | Noto Sans SC / PingFang SC / Microsoft YaHei 中文回退；标题与正文层级明确，设备名和状态未遮挡；窄屏隐藏导航装饰图标，保留文字 |
| 间距布局 | 五页公用导航和标题区；首页三卡与两栏结构；低高度笔记本四画面完整可见；窄屏单列、局部列表横向滚动 |
| 颜色 | 深蓝灰背景，蓝色选中，橙色火情，黄色设备异常，绿色完成；状态同时有文字，不只依赖颜色 |
| 图片 | 使用原项目照片，保持比例，逐画面显示历史样张标签；未用新绘制图片冒充监控画面 |
| 文案 | 示例数据标记持续可见，去除“空地协同封控中”等无业务依据的全局状态；技术性授权到期解释改为用户能理解的名额切换结果 |

## 发现、修复与复查历史

1. [P1，已修复] 初始底图服务返回 API key 水印瓦片。改用标准 OSM 瓦片与常显署名；最终总览、任务截图显示真实底图，无水印与网络错误提示。
2. [P2，已修复] 1280×720 初版视频四分屏第二行落在首屏之外。增加低高度视口规则，限制视频区域高度；同视口 `02-video.jpg` 四画面完整显示，控制栏位于下方。
3. [P2，已修复] 筛选后详情可能停留在已被过滤的事件/任务。跟随筛选选取可见条目，无结果时隐藏详情；实测事件无结果时 `detailVisible=false`，任务“已完成”显示西区热源复查。
4. [P2，已修复] 切换任务时地图飞机点位未监听 devices 变化。增加依赖监听；筛选已完成任务后，地图只留下巡护 06 号。
5. [P2，已修复] 二十路总览在低高度窗口需要滚动才能看到末行。明确限制父子网格最小高度并使用四行等分；1280×720 复查 20 个画面均在首屏，最下方边界 655px，4 个高清名额保持不变。
6. [P2，已修复] 原地图默认 aria-label 覆盖语义，及“待复核”图例同时展示处理中火情。恢复具体点位名称，图例改为“待处理火情”；最终任务截图与浏览器语义树确认。

## 已执行交互验证

- 五个顶部入口及 hash 导航可用；切换页面滚动回顶部。
- 地图事件 → 事件详情 → 对应飞机单路画面 → 关联事件 → 任务详情跳转一致。
- 复核空说明时按钮禁用；填写说明并保存，状态变为处理中、记录增加、首页待复核数量从 3 变为 2。
- 第 5 架替换第 1 架高清后，二十路总览仍有 20 个画面，其中 4 高清、16 低清。
- 任务筛选与地图联动；异常设备筛选恰好两架，详情展示当前飞机，Esc 可关闭并恢复焦点。
- 设备与事件搜索无结果时显示空状态，事件详情不残留；恢复筛选和重置演示有效。
- 五页在 390×844 下均无整页横向溢出（document scrollWidth=384，viewport=390）；设备表格只在容器内横向滚动。
- 浏览器检查无 error；仅记录到修复前的 Vue feature flag 警告，已在 Vite 配置中补齐 flags。
- 独立 Vite 构建通过。MapLibre 使 JS 包超过默认体积建议值，原型阶段接受；正式整合应按路由拆包。

## 剩余范围与清单

- [x] 核对视觉层级、图片、文案、地图署名与窄屏布局。
- [x] 修复上述 P1/P2，并在实际浏览器重复检查。
- [x] 不修改正式路由、不连接真实飞机或写入真实业务数据。
- [P3] 正式接入时补齐真实空数据、离线、权限和订阅生命周期；按项目既有业务服务实现，不沿用示例数据。
- 原型未进行真实视频播放、飞行、20 设备压测或跨浏览器兼容性验收；浏览器全屏功能仍取决于运行环境许可。

历史五页原型结果：passed


# 2026-09-06 视频工作区交互改版

本节为当前版本结论。保留此前四个非视频页面；原固定视频分屏改为右侧缩略图、左侧自由播放窗口。

## 依据与截图

- 用户截图原件：`/var/folders/hy/k25g8vts3ml0bkmwpfcg_68w0000gn/T/codex-clipboard-610c28a5-d5f3-4bab-9796-9758ba6d0b3e.png`，2048×1022；本地证据副本 `qa/source-video-user.png`。
- 最终桌面图：`qa/09-video-workspace-desktop.jpg`，CSS 视口 1280×800，截图工具输出约 1274×795。
- 窄屏图：`qa/10-video-workspace-mobile.jpg`，CSS 视口 390×844，输出约 384×831。
- 实现 URL：`http://127.0.0.1:5188/#video`。
- 用户截图与实际桌面图在同一比较输入中打开。原图显示单个大画面、文字飞机列表，新图按用户要求变为可叠放的多画面与图片缩略图；这是明确的设计变更，不要求保持单画面比例。
- 工具返回接近 CSS 尺寸的截图，没有另外缩放；原参考尺寸、滚动位置与示例状态不同，因此不做逐像素相似度判断。桌面图中单窗标题、操作按钮和缩略图均可读；拖动、缩放、放大后的重点区域通过真实浏览器动作和 DOM 边界验证。

## 五项表面检查

- 字体：沿用现有中文字体栈、标题与正文层级；小窗缩减辅助标签，保留飞机名称及关闭/放大按钮。
- 布局：桌面右侧固定宽度缩略图，左侧伸缩播放区；窗口采用归一化坐标；窄屏缩略图位于下方，整页无横向溢出。
- 颜色：沿用深蓝灰、蓝色关注框和高清标签，暖色用于弱网；没有添加新视觉主题。
- 图片：复用原历史图片，保持主画面比例；缩略图适度裁切并明确标注历史样张，不冒充实时视频。
- 文案：说明拖标题移动、拖右下角缩放；“申请高清关注”独立于窗口大小；不向页面用户展示内部租约和协议实现。

## 发现与修复记录

1. [P2，已修复] 平铺后当前选中窗口可能处于其他窗口下层。改为每次操作把目标窗口置顶；浏览器验证选中窗口 z-index 与全体最高值一致。
2. [P2，已修复] “重置演示”可能被组件卸载时的保存覆盖。改为卸载后清理存储；验证重置后回到四画面与四个初始高清名额。
3. [P2，已修复] 关闭全部窗口后刷新可能重新自动添加默认飞机。区分无关注画面与显式打开设备；验证空布局刷新后仍为空，可用恢复四画面或缩略图添加。
4. [P2，已修复] 展开工作区时需保留键盘焦点，嵌套的高清弹窗 Esc 不应同时退出工作区。增加焦点循环、恢复和弹窗优先处理。第一次实测发现不同监听器间的 DOM 更新时序仍会导致同时退出，进一步由外层 Escape 处理设置 preventDefault、内层尊重该标记；再次实测 expandedPreserved=true、quotaDialogClosed=true，焦点循环也通过。

## 实际验证

- 从右侧第 5 架飞机缩略图真实拖入左侧：窗口数 4 → 5，当前关注第 5 架；重复拖入仍为 5。
- 拖标题改变位置；拖右下角尺寸从约 472×242 变为 557×284；方向键也可移动和缩放。
- 将标题与缩放手柄拖到播放区以外，窗口四边均限制在播放区内。
- 页面切换后窗口位置/大小/层级一致；刷新后位置和大小一致。
- 单窗放大时只显示一个大窗，还原后其他窗口恢复；关闭后对应缩略图可再次添加。
- 一键平铺保留当前已选飞机；布局几何检查覆盖 1–20 个窗口的边界和不重叠条件。
- 第 5 架申请高清、让出第 1 架名额后，仍为 4 高清 + 16 低清；仅拖入或放大不改变名额。
- 搜索无结果时显示清除入口，不影响已打开窗口；关闭全部画面有添加提示。
- 390×844 下 document.scrollWidth=384，没有整页横向溢出。小屏以点击添加作为拖拽的替代入口。
- 浏览器 error 日志为空；独立 Vite 生产构建通过。仍有原型包含 MapLibre 的体积建议警告。

## 接入边界

本轮验证的是样张布局、输入交互与演示状态。缩略图的真实播放、接收流共享、可见范围订阅、高清授权生效和设备性能验收属于正式媒体接入阶段，具体要求已写入 README。

历史视频工作区结果：passed


# 2026-09-06 火情事件三栏改版

当前版本以本节为准。

## 比较依据与证据

- source visual truth path：`qa/source-events-before.png`（图 1，3584×1788）与 `qa/source-events-timeline.png`（图 2，1686×1188）。来自本轮用户附件的原始副本。
- implementation：`http://127.0.0.1:5188/#events`。
- implementation screenshot path：`qa/11-events-desktop.jpg`，1280×800 CSS 视口，DPR 1，工具输出 1274×796 JPEG。
- 宽屏：`qa/15-events-wide.jpg`，1792×900 CSS 视口，工具输出 1786×897；三栏宽度实测 280 / 986 / 390px。1280px 视口实测为 250 / 574 / 350px。
- 窄屏：`qa/13-events-mobile.jpg`、`qa/14-events-mobile-timeline.jpg`，390×844 CSS 视口，工具输出 384×831。
- 重点状态：`qa/12-events-handling-details.jpg` 展示已关闭事件的结果和详情；`qa/16-events-review-result.jpg` 展示本轮复核新增节点。
- state：默认待复核事件 F-0905-003、现场图片、全部记录展开；另验证处理中、已排除、已结束、空筛选和人工复核后的状态。
- density normalization：工具输出已接近 CSS 尺寸，未再缩放文件。图 1 原始尺寸约为宽屏视口的两倍；图 2 是另一系统和不同事件状态。比较三栏信息架构、原深色风格及时间轴字段，不声称逐像素重现图 2。
- full-view comparison evidence：原始图 1、图 2 与桌面实现、已关闭详情截图在同一次图片输入中打开比较。修正文案后，图 2 与最终桌面、复核结果截图再次在同一次输入中比较。
- focused region comparison evidence：详情滚动截图和复核截图单独覆盖人员、时间、内容、结果及关联任务；手机时间轴截图进一步验证字段换行和层级，因此无需再裁切完整桌面图。

## 视觉检查

- 字体：沿用原型中文字体栈和 Ant Design 图标，标题、事件名、状态、时间和内容层级清晰；右栏说明自然换行。
- 间距布局：列表收窄、现场图居中、时间轴移到右侧。左右区域独立滚动，主要操作固定在各自面板底部；桌面图中图片、时间轴和复核入口同屏。
- 颜色：保持原深蓝灰底色、蓝色选中、橙色待复核和绿色已关闭，未套用参考图 2 的青色主题。
- 图片：沿用既有照片，主图使用 contain 保留完整证据，宽屏出现留边属于避免裁切的预期表现；缩略图适度裁切。持续标注历史样张，红外参考样张明确区别于当前事件证据。
- 文案：节点展示人员或提醒对象、时间、处理内容、结果；不预先填充未完成的处置节点。样例单位、负责人和提醒记录与页面示例数据标记一致。

## 发现、修复与复查

1. [P2，已修复] 第一版将全部事件信息放在右栏上部，挤压首屏时间轴。将事件类型、级别、说明和任务移至时间轴下方，顶部只保留进展和负责人。桌面及宽屏截图复查可见时间轴占据右栏主体。
2. [P2，已修复] 人工确认后，顶部显示等待安排、底部仍显示等待现场反馈。统一使用当前派遣状态生成提示，并将通用任务入口命名为“查看关联任务”。复核后实测上方“已确认火情，等待安排处置”、下方“等待安排现场处置”，见 `16-events-review-result.jpg`。
3. [P2，已修复] 小高度桌面使用 510px 最小区域容易超出首屏。下调最小高度至 440px，1280×800 最终三栏和主要操作完整显示。

## 已验证交互

- 切换事件同步图片、处置状态、人员、记录及关联任务；右侧滚动位置重置为 0。
- 现场/红外参考图片切换、放大弹窗及 Esc 关闭通过；仅红外事件显示一个图片选项。
- 四节点事件全部收起后四个 aria-expanded 均为 false，全部展开恢复；单条记录有独立按钮与关联内容 ID。
- 人工复核空说明不能提交；填写后记录由 2 增至 3，状态变为处理中，待办由 3 减至 2，新增记录包含时间、人员、说明及结果。
- 搜索无结果时场景和右栏均消失，出现清除筛选入口；清除后恢复。已关闭筛选包含已排除和已结束。
- 右栏实测滚动 416.5px 时中间场景位置保持不变；下方事件详情和完成结果可达。
- 关联任务跳转至 T-0904-06“西区热源复查”，返回关联事件后点击现场画面聚焦巡护 06 号；检查后恢复测试新增的视频窗口。
- 手机 390×844 下文档宽度 384px，无整页横向溢出；事件列表横滑，图片、时间轴和详情纵向排列。
- 浏览器 error 日志为空；最终独立 Vite 生产构建通过，仍有既有 MapLibre 大包体积提示。

## 结果与边界

- [x] 已完成布局、图片、时间轴详情、筛选、复核与页面跳转检查。
- [x] 原型 README 同步更新；正式业务路由、服务及设备调用未变更。
- 原型仍使用固定日期和示例数据；未连接真实通知、现场处置或媒体服务。本次验证限于桌面内置浏览器及所测视口。
- 无待解决 P0/P1/P2。后续可按真实处置流程细化字段，不影响本次布局交付。

previous revision result: passed


# 2026-09-06 功能入口扩展版

## 验证范围与设计依据

在已确认的深色原型上增加任务中心、专业作业和平台管理。没有推翻现有火情三栏、视频自由窗口。沿用原样式变量、Ant Design 图标和既有历史图片，规划使用 MapLibre 与真实 OSM 底图。

- 同视口、同事件对照：`qa/15-events-wide.jpg` 与 `qa/24-events-wide-preserved.jpg`（1792×900），作为一组图片直接审阅。三栏宽度、现场图片、时间轴、底部复核位置保留；差异是顶部增加专业作业和管理入口、任务/设备名称调整。
- 新工作区桌面审阅：1440×960 的 `17-route-library.jpg`、`18-route-planner.jpg`、`19-create-mission.jpg`、`20-execution-history.jpg`、`21-professional-flight.jpg`、`22-delivery-complete.jpg`。面板间距和文字层级与旧版一致；地图点线可读，图片保持比例，较长工作区正常纵向滚动。
- 窄屏：390×844 的 `25-planner-mobile.jpg`，上下排列设置、地图和航点；导航栏独立横滑，实测文档宽 384px，无整页横向溢出。
- 1280×800 回查：`26-repeat-independent.jpg`、`27-video-preserved.jpg`；视频四窗口与缩略列表保持原结构。

## 已验证交互

- 航线编辑改名称、航高 100→120，并应用到全部航点；开启添加后点击地图，航点 6→7；撤销后通过右栏再次新增，保存后库内显示新名称与 7 航点。
- 经度 999 的越界值触发说明并禁止保存，地图过滤无效点，没有浏览器错误。坐标恢复后可继续操作。
- 创建独立巡检任务后模拟下发，显示 1/7 航点；暂停、继续、完成依次有效；历史列表保留完成记录。
- 再次执行生成新的 T-DEMO 编号待执行任务，原 T-0904-06 已完成记录同时保留，见 `26-repeat-independent.jpg`。
- 导入内置北区示例出现 6 个航点，可检查并保存；不宣称实现真实 KMZ 解析。
- 飞行控制需申请演示控制权后才能模拟悬停；投送填写说明/勾选资源后提交、通过审批、启动、完成；跳转关联火情后返回，已完成状态保留。
- 媒体资料可打开预览并跳到关联火情；火情仍显示正确现场、时间轴和任务链接。
- 视频页仍显示 4 个窗口、4 高清/16 低清。原视频组件及布局算法没有修改，本次不重复此前完整拖动矩阵。
- 浏览器 error 日志为空。最终独立 Vite 生产构建通过（既有 MapLibre 大包提示，不阻断构建）。

## 发现与修正

1. 航点数值编辑可能产生无效坐标：绘图过滤非法点，保存前校验坐标、航高与速度。
2. 专业作业跳到事件后可能丢失审批状态：投送状态移至共享内存，跨主页面返回仍显示完成结果。
3. 六个顶级导航在窄屏下拥挤：主导航和二级导航独立横滑，规划设置与航点面板转为纵向排列。

## 边界

评审目标是补齐功能位置与核心模拟流程；固件、成员、空域等为辅助管理样例，真实设备控制、KMZ、媒体服务及完整业务迁移均未实施。详见 `feature-coverage.md`。无需真实飞行验收来评审本次原型。

final result: passed
