# M4T双流直播与火情识别专项详细设计方案（评审修订版）

## 1. 文档说明

### 1.1 文档目的

本文档用于形成一份可进入技术评审和 PoC 实施阶段的《M4T 双流直播与火情识别专项详细设计方案》。

本方案聚焦以下专项能力：

1. M4T 可见光与红外热成像双流获取与双流推送。
2. 驾驶舱前端双画面实时播放。
3. AI 同时消费可见光与红外两路数据进行火情识别与复核。
4. 双流证据留痕，为后续火点定位与 FC100 处置接口预留数据基础。

本文档不覆盖完整灭火全流程，不替代《无人机火情发现与灭火联动系统详细设计方案》，而是作为其“媒体与识别子系统专项方案”。

---

## 2. 文档边界与不在范围内事项

为避免方案边界模糊，现明确本方案范围。

### 2.1 本方案范围

本方案包含：
- M4T 双流获取前提分析
- RC Plus 2 上 MSDK 执行层设计
- 双流推流与播放设计
- 双流 AI 融合识别设计
- 双流证据模型设计
- PoC 验证指标与通过门槛
- 双流失败时的退化方案

### 2.2 不在本方案范围内

以下能力不在本专项方案中闭环实现，仅做接口预留或依赖上位方案：

1. Cloud API 航线规划与巡航任务总控。
2. FC100 跨机协同调度。
3. FC100 飞抵火点后的空域避让策略。
4. 火点世界坐标高精度反投算法。
5. 灭火负载控制与处置闭环。
6. 热成像原始温度矩阵获取方案的最终工程定型。

说明：
- 本方案可以为火点坐标计算提供必要证据字段，但不承诺在本专项内完成“火点坐标派发 FC100 并稳定处置”的全链路闭环。
- 本方案可作为 FC100 联动的输入基础，但不等同于完成跨机灭火联动。

---

## 3. 关键前提条件

## 3.1 本方案不是纯 Cloud API 原生双流方案

需要首先明确：

### 3.1.1 现状说明
当前 DJI Cloud API / Pilot 2 原生直播能力是按单个 `video_id` 启动一路推流的模型。`live_lens_change` 的能力本质是切换直播镜头，而不是原生并发双路推流。

因此，不能将“Cloud API 可切镜头”理解为“Cloud API 原生支持双流并发推送”。

### 3.1.2 本方案采用的技术前提
本方案的双流实现前提是：

> **RC Plus 2 上允许部署并运行自研轻型 App，作为 MSDK 执行层，用于获取和推送双路视频源。**

该自研轻型 App 可以有两种部署形态：

#### 形态 A：Pilot 2 共存式轻型 App
- 保留 Pilot 2 承担飞行任务主流程和 Cloud API 接入。
- 通过第三方轻型 App 承担近端双流获取与推流职责。

#### 形态 B：完整自研 Pilot App
- 用自研 Android App 替代 Pilot 2 的部分或全部前台能力。
- MSDK 承担全部近端取流和控制逻辑。

本方案优先推荐 **形态 A（Pilot 2 共存式轻型 App）** 进入 PoC，以降低替换 Pilot 2 的风险和改造成本。

### 3.1.3 关键约束
若现场环境、设备策略或产品形态不允许在 RC Plus 2 上部署自研轻型 App，则本方案不成立，需退回到“单流 + 切镜头”方案。

---

## 3.2 本方案适用部署形态

本方案默认适用于以下场景：

- 设备：Matrice 4T
- 遥控器：DJI RC Plus 2
- 形态：手持遥控飞行场景
- 近端执行：RC Plus 2 上运行第三方轻型 App（MSDK 执行层）
- 平台总控：Cloud API / 后端调度中枢

本方案不直接适用于以下场景：

- 纯 Dock 场景且无 RC Plus 2 自研执行层。
- 完全不允许部署第三方 App 的现场环境。
- 仅依赖 Pilot 2 原生直播能力、无自研 App 的场景。

---

## 4. 设计目标

## 4.1 业务目标

1. 驾驶舱页面同时显示可见光与红外热成像两路实时画面。
2. AI 服务同时获取两路数据进行火情识别与红外复核。
3. 火情识别不再依赖“切镜头后再复核”，而是基于双流并行做融合判定。
4. 为后续火点定位、事件固证和 FC100 联动提供完整的双流证据基础。

## 4.2 技术目标

1. 在 RC Plus 2 上同时获取可见光与红外对应的视频源或帧源。
2. 同时向媒体中枢推送两路直播流。
3. 前端双窗口低延迟播放。
4. AI 同时消费两路流并完成时间对齐、空间关联和风险评分。
5. 运行状态、证据数据和健康状态可观测、可追踪、可评估。

---

## 5. 总体架构设计

## 5.1 总体架构

```text
网页端驾驶舱
    ↓
业务后端 / 调度中枢
    ├─ Cloud API 接入层
    │    ├─ 设备接入
    │    ├─ 飞行任务编排
    │    └─ 状态汇聚
    │
    ├─ RC Plus 2 第三方轻型 App（MSDK 执行层）
    │    ├─ 可见光视频源获取
    │    ├─ 红外视频源获取
    │    ├─ 双路码流监听
    │    ├─ 双路帧监听
    │    ├─ 双路推流控制
    │    └─ 健康状态回传
    │
    ├─ 媒体中枢（ZLMediaKit）
    │    ├─ visible 流接入
    │    ├─ thermal 流接入
    │    ├─ WebRTC 播放出口
    │    ├─ HTTP-FLV 播放出口
    │    └─ AI 拉流入口
    │
    ├─ AI 双流分析服务
    │    ├─ 可见光火焰/烟雾检测
    │    ├─ 红外热点分析
    │    ├─ 时间同步与缓冲
    │    ├─ 空间近邻匹配
    │    └─ 融合评分
    │
    └─ 事件中心
         ├─ 双流证据归档
         ├─ 告警生成
         └─ 联动接口预留
```

---

## 5.2 职责划分

### 5.2.1 Cloud API 职责
Cloud API 仅承担平台总控职责：
- 设备接入
- 飞行任务下发
- 状态收集
- 上层业务编排

Cloud API 不承担双路视频并发推送实现。

### 5.2.2 MSDK 执行层职责
MSDK 执行层承担：
- 双路视频源探测
- 双路码流/帧获取
- 双路推流
- 局部相机/镜头能力调用
- 健康状态回传

### 5.2.3 ZLMediaKit 职责
- 接收双路 RTMP/RTSP
- 输出双路 WebRTC / HTTP-FLV
- 向 AI 提供统一的标准拉流地址
- 统一双流链路，避免一路 Agora、一路 ZLMediaKit 的混搭

### 5.2.4 AI 服务职责
- 同时消费 visible / thermal 两路输入
- 做时间同步、空间匹配和融合评分
- 输出疑似火情和确认火情事件

---

## 6. 双流实现前提分析

## 6.1 现有能力判断

MSDK 图传能力已支持：
- 获取可用相机索引
- 指定相机索引接收原始码流
- 指定相机索引接收帧数据
- 多 Surface 输出
- 相机视频源切换

从能力模型上，可以认为系统具备向双流架构演进的 SDK 基础。

但需要明确：

> **能力模型具备基础 ≠ M4T 当前实际组合一定稳定支持双流并发推送。**

---

## 6.2 必须验证的关键问题

PoC 前必须明确下列问题：

1. M4T 在当前模式下，是否同时暴露可见光与红外两个可独立订阅的视频源。
2. RC Plus 2 上的第三方轻型 App 是否能同时订阅两路流而不冲突。
3. 两路流是否能同时编码/转发，且不会因硬件资源限制严重掉帧。
4. 双路帧数据是否都能持续稳定提供给 AI 服务。
5. 双路视频在 30 分钟以上的连续运行中是否稳定。

---

## 7. PoC 设计与通过标准

## 7.1 PoC 目标

本专项方案必须先进行 PoC，PoC 成功后方可进入正式开发。

PoC 验证目标如下：

1. 同时获取 visible 与 thermal 两路有效视频源。
2. 同时将两路流推送到 ZLMediaKit。
3. 驾驶舱页面可同时低延迟播放两路流。
4. AI 服务可同时拉取两路流并做基础对齐分析。
5. 运行过程中遥控器端资源占用、温升、延迟和丢帧在阈值内。

---

## 7.2 PoC 通过标准

建议以如下量化指标作为通过门槛：

### 7.2.1 媒体链路指标
- 双流端到端延迟：**≤ 1.5 秒**
- 单路端到端延迟波动：**≤ 300 ms**
- 双流连续播放丢帧率：**< 2%**
- 双流均可持续运行：**≥ 30 分钟**

### 7.2.2 遥控器侧资源指标
- RC Plus 2 CPU 平均占用：**不高于既定阈值（PoC 建议 75%）**
- 内存占用稳定，无持续泄漏
- 设备温度持续运行 30 分钟不触发高温保护

### 7.2.3 AI 输入可用性指标
- visible / thermal 两路帧均可稳定获取
- 两路帧平均时间偏差在设定同步窗口内
- AI 基础融合识别可运行，无持续阻塞

### 7.2.4 结果判定
- 全部关键指标满足门槛：判定 PoC 通过
- 任一核心项失败：判定双流方案不通过，进入退化方案评审

---

## 8. 退化方案设计

## 8.1 退化前提

若 PoC 结果表明双流并发推送不可稳定实现，则系统退化为：

> **单流 + 镜头切换方案**

但必须强调：

> **退化方案不等价于双流方案，AI 策略必须单独设计。**

---

## 8.2 退化方案能力说明

退化后系统仅保留：
- 单一路直播流
- 通过镜头切换实现可见光 / 红外切换
- 基于切换后的画面进行复核

不再具备：
- 两路同时展示
- 两路同步融合识别
- 双流持续时间窗口复核

---

## 8.3 退化方案 AI 策略

在单流退化方案下，AI 处理策略必须改为两阶段：

### 第一阶段：可见光巡检
- 火焰/烟雾识别
- 连续帧验证
- 生成疑似火点事件

### 第二阶段：切红外复核
- 切换红外后重新建立时间窗口
- 执行热点判断与热异常识别
- 生成复核结论

说明：
- 退化方案中不存在“双流时空融合”，而是“疑似发现 + 复核确认”的串行流程。

---

## 9. 双流媒体设计

## 9.1 流命名设计

考虑到 `drone_sn` 本身具备全局唯一性，为避免同一架飞机切换 workspace 导致流 ID 改变，本方案采用以下规则：

### 9.1.1 流组 ID
- `stream_group_id = {drone_sn}`

### 9.1.2 子流 ID
- `visible_stream_id = {drone_sn}_visible`
- `thermal_stream_id = {drone_sn}_thermal`

示例：
- `m4t001_visible`
- `m4t001_thermal`

---

## 9.2 协议设计

### 9.2.1 近端到媒体中枢
建议采用 RTMP 作为 RC Plus 2 到 ZLMediaKit 的推流协议：
- 成熟稳定
- 双实例推流易于实现
- ZLMediaKit 接入成本低

### 9.2.2 媒体中枢到前端
建议统一采用：
- 主方案：WebRTC
- 备选：HTTP-FLV

### 9.2.3 媒体中枢到 AI
建议统一采用：
- RTSP 或 RTMP 拉流

### 9.2.4 统一链路原则
本方案明确要求：

> **双流统一走 ZLMediaKit，不采用一路 Agora、一路 ZLMediaKit 的混搭。**

原因：
- 可显著降低时间同步复杂度
- 降低前端播放器异构成本
- 降低 AI 双流对齐难度

---

## 10. 时间同步设计

## 10.1 问题说明

双流融合识别的关键难点之一不是“能否同时播放”，而是：

> **可见光与红外是否在同一时间基准下可对齐。**

如果两条链路延迟不同、缓冲不同、转发不同，仅靠“时间窗口 + 缓冲队列”是不够的，必须定义明确的同步锚点。

---

## 10.2 同步锚点设计

建议按以下优先级设计同步锚点：

### 第一优先级：原始帧时间戳 / PTS
若 RC Plus 2 侧双路帧可获取稳定的原始时间戳或等价 PTS，则以该时间戳作为主同步锚点。

### 第二优先级：近端统一采集时间戳
若原始 PTS 不可直接可靠使用，则在 MSDK 执行层接收到帧/码流时，统一打上近端采集时间戳，作为双流统一锚点。

### 第三优先级：飞机 OSD 时间与位置辅助校验
结合以下字段做辅助校验：
- 飞机经纬度
- 高度
- 云台姿态
- 当前飞行时间点

---

## 10.3 同步策略

### 10.3.1 AI 同步缓冲队列
AI 服务需分别为 visible 和 thermal 建立缓冲队列，并按同步锚点对齐。

### 10.3.2 对齐窗口
建议初始配置：
- 对齐容忍窗口：**±300ms**
- 若超出阈值，则该批次不做严格融合，只做弱关联判定

### 10.3.3 同步失败降级
若某时刻两路数据无法对齐：
- 不直接中断任务
- 降级为单路可信度输出
- 记录同步失败日志供运维分析

---

## 11. 双流 AI 识别设计

## 11.1 可见光分析子模块

职责：
- 火焰识别
- 烟雾识别
- 目标跟踪
- 连续帧稳定性验证

输出：
- visible_bbox
- class_name
- confidence
- track_id
- source_ts

---

## 11.2 红外分析子模块

本模块需分两种实现等级：

### 11.2.1 等级 A：仅基于红外视频流伪彩图分析
适用于当前只拿到红外视频流、无法获取原始热数据的场景。

能力：
- 热对比度分析
- 异常热点区域提取
- 热斑持续性分析

限制：
- 无法获得真实温度值
- 无法做高精度温度阈值判定
- 精度明显受限

### 11.2.2 等级 B：基于帧级热数据或原始温度矩阵分析
适用于后续若 MSDK/设备能力允许获取热成像原始数据的场景。

能力：
- 基于温度矩阵的热点检测
- 绝对温度阈值分析
- 高热区域精细判断

说明：
- 本评审修订版明确要求：**若当前仅能获取红外视频流，则必须在方案中声明“热分析为伪彩图近似分析”，不得表述成真实温度分析。**

---

## 11.3 双流融合判定模块

### 11.3.1 融合输入
- visible 分析结果
- thermal 分析结果
- 同步锚点时间
- 飞机位姿信息
- 云台姿态信息

### 11.3.2 融合逻辑
1. 时间对齐
2. 空间近邻匹配
3. 风险评分
4. 事件状态流转

### 11.3.3 输出等级
- LOW：疑似异常
- MEDIUM：可见光或红外单侧持续异常
- HIGH：双流同时异常且稳定匹配

---

## 12. 前端设计

## 12.1 驾驶舱布局

建议采用主副画面布局：
- 主画面：默认可见光
- 副画面：默认红外
- 支持一键主副互换

### 12.1.1 双画面功能
- 双路实时播放
- 双路状态显示
- 双路截图
- 双路同步告警标注
- 当前焦点画面切换

### 12.1.2 告警交互
当 AI 在 visible 发现疑似火点时：
- 主画面高亮目标
- 副画面自动高亮红外对应区域
- 告警面板提示“双流复核中”

---

## 13. 后端设计

## 13.1 运行态管理原则

双流组和双流明细本质上属于运行态动态资源。为避免与真实状态脱节：

### 13.1.1 Redis 存运行态
建议使用 Redis 存放：
- 流组状态
- 流在线状态
- 双流健康状态
- 当前播放焦点
- 双流同步状态

### 13.1.2 数据库存历史证据
数据库仅存：
- 火情事件
- 双流证据记录
- 截图
- 录像片段
- 人工复核结果

---

## 13.2 接口设计

### 13.2.1 查询双流信息
**GET** `/api/v1/live/groups/{drone_sn}`

返回示例：
```json
{
  "stream_group_id": "m4t001",
  "visible": {
    "stream_id": "m4t001_visible",
    "play_url_webrtc": "webrtc://media/live/m4t001_visible",
    "play_url_flv": "https://media/live/m4t001_visible.live.flv",
    "status": "ONLINE"
  },
  "thermal": {
    "stream_id": "m4t001_thermal",
    "play_url_webrtc": "webrtc://media/live/m4t001_thermal",
    "play_url_flv": "https://media/live/m4t001_thermal.live.flv",
    "status": "ONLINE"
  },
  "sync_status": "NORMAL"
}
```

### 13.2.2 启动双流推送
**POST** `/api/v1/live/groups/{drone_sn}/start`

### 13.2.3 停止双流推送
**POST** `/api/v1/live/groups/{drone_sn}/stop`

### 13.2.4 切换主副焦点
**POST** `/api/v1/live/groups/{drone_sn}/focus`

请求示例：
```json
{
  "focus_stream": "thermal"
}
```

---

## 14. 证据模型设计

## 14.1 设计要求

原方案仅存双流截图和 bbox，不足以支撑后续火点定位和联动。因此本修订版明确要求补齐关键上下文字段。

---

## 14.2 双流证据表设计 fire_dual_stream_record

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| event_id | varchar(64) | 火情事件编号 |
| drone_sn | varchar(64) | 无人机编号 |
| source_ts | datetime | 统一时间锚点 |
| visible_screenshot_url | varchar(512) | 可见光截图 |
| thermal_screenshot_url | varchar(512) | 红外截图 |
| visible_bbox | text | 可见光框 |
| thermal_bbox | text | 红外框 |
| fusion_score | decimal(5,4) | 融合分值 |
| aircraft_lng | decimal(12,8) | 飞机经度 |
| aircraft_lat | decimal(12,8) | 飞机纬度 |
| aircraft_alt | decimal(8,2) | 飞机高度 |
| gimbal_pitch | decimal(8,3) | 云台俯仰角 |
| gimbal_yaw | decimal(8,3) | 云台偏航角 |
| gimbal_roll | decimal(8,3) | 云台横滚角 |
| camera_mode | varchar(32) | 镜头模式 |
| zoom_ratio | decimal(8,3) | 变焦倍率 |
| focal_length | decimal(8,3) | 焦距（若可获取） |
| fov | decimal(8,3) | 视场角（若可获取） |
| visible_pts | bigint | 可见光时间戳 |
| thermal_pts | bigint | 红外时间戳 |
| create_time | datetime | 创建时间 |

说明：
- 以上字段为后续火点定位与处置联动预留。
- 本专项不承诺直接完成坐标反投，但必须保证证据字段完整。

---

## 15. 风险与应对

## 15.1 最大风险清单

### 风险一：双流能力 PoC 不通过
应对：
- 严格按量化门槛评估
- 及时转入单流退化方案

### 风险二：遥控器资源不足
应对：
- 降低码率
- 降低分辨率
- 一路做直播，一路只取帧不推流作为兜底方案

### 风险三：双流时间不同步
应对：
- 统一时间锚点
- 增加对齐窗口
- 记录同步偏差监控指标

### 风险四：红外仅伪彩图，热分析精度不足
应对：
- 在方案中明确热分析等级
- 后续单列“原始热数据链路”优化项

### 风险五：读者误以为本方案已覆盖 FC100 灭火联动
应对：
- 明确边界
- 联动仅做接口预留
- FC100 协同另立专项方案

---

## 16. 分期实施建议

## 16.1 第一阶段：前提确认
- 确认 RC Plus 2 可部署第三方轻型 App
- 确认 Pilot 2 共存形态可行
- 确认 MSDK 版本与 M4T 当前固件可配合

## 16.2 第二阶段：PoC 验证
- 双源监听
- 双路推流
- 双路播放
- 双路 AI 拉流
- 连续运行与温升评估

## 16.3 第三阶段：双流直播版
- 双流组管理
- 驾驶舱双播放
- 双流启停
- 主副聚焦切换

## 16.4 第四阶段：双流识别版
- visible 检测
- thermal 热分析
- 时间同步与融合判定
- 双流证据归档

## 16.5 第五阶段：联动接口预留
- 向事件中心输出标准火情事件
- 向定位/处置系统输出完整证据数据
- 为 FC100 联动预留接口

---

## 17. 最终结论

本评审修订版的核心结论如下：

1. **本方案不是 Cloud API 原生双流方案，而是基于 RC Plus 2 第三方轻型 App（MSDK 执行层）的双流专项方案。**
2. **接入 MSDK 后系统具备双流实现方向，但是否能稳定落地，必须通过真机 PoC 验证。**
3. **若 PoC 通过，则采用“visible + thermal 双流并行、统一走 ZLMediaKit、AI 双流融合识别”的正式方案。**
4. **若 PoC 不通过，则退化为“单流 + 镜头切换”，并采用独立 AI 策略，不宣称与双流等价。**
5. **本方案只解决双流直播与双流识别专项问题，不承诺在本专项内完成 FC100 跨机处置闭环。**

---

## 18. 一句话结论

这份修订版方案的目标，不再是“画一个理想双流蓝图”，而是给出一条可以直接进入 PoC 的真实工程路径：先明确部署前提，再做定量 PoC，PoC 成功则落双流正式版，失败则进入单流退化版，并把时间同步、证据字段和处置边界一次性说清楚。

