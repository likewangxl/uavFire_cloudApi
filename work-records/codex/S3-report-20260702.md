# S3 前端事件处置工作台骨架报告

日期：2026-07-02

## 实现范围

- 新增前端页面：`frontend/src/pages/page-web/projects/operation-workbench.vue`
- 新增组件：
  - `frontend/src/components/operation/IncidentListPanel.vue`
  - `frontend/src/components/operation/OperationMap.vue`
  - `frontend/src/components/operation/IncidentDetailPanel.vue`
  - `frontend/src/components/operation/OperationTimeline.vue`
  - `frontend/src/components/operation/OperationActionBar.vue`
- 新增 operation API/types/mock：
  - `frontend/src/api/operation/client.ts`
  - `frontend/src/api/operation/incident.ts`
  - `frontend/src/api/operation/mock.ts`
  - `frontend/src/types/operation/api.ts`
  - `frontend/src/types/operation/incident.ts`
- 新增共享策略：
  - `frontend/src/pages/page-web/projects/operation-policy.mjs`
  - `frontend/src/pages/page-web/projects/__tests__/operation-workbench-policy.test.mjs`
- 注册路由/菜单：
  - `frontend/src/types/enums.ts`
  - `frontend/src/router/index.ts`
  - `frontend/src/components/common/sidebar.vue`
  - `frontend/src/pages/page-web/projects/workspace.vue`
- 地图复用：
  - `CockpitSituationMap.vue` 新增 `showUomAirspace` 可选 prop，默认不启用；事件工作台通过开关启用 UOM 参考层。

## 后端契约确认

已读取 `backend/uavfire/src/main/java/com/yx/uavfire/fc100/operation/controller/OperationIncidentController.java` 及 operation DTO/param/entity/state machine：

- 列表、详情、timeline、assign-monitor、assign-delivery、dispatch、abort、close、mark-false-alarm 已按真实路径接入。
- `CONFIRM` 在状态机存在，但 controller 当前未暴露确认接口；前端 `确认火情` 按 S3 骨架占位置灰。
- `dispatch` 仅在 `CONFIRMED` 且存在 active `DELIVERY_PRIMARY` assignment 时可见。
- `abort`、`mark-false-alarm`、`archive/close` 按后端允许状态展示，并全部走二次确认；`abort` 与 `mark-false-alarm` UI 要求原因必填。

## Mock 开关

- 环境变量：`VITE_OPERATION_MOCK=true`
- 或浏览器 localStorage：`uavfire_operation_mock=1`
- Mock 模式支持列表、详情、timeline、dispatch、abort、close、mark-false-alarm、assign-monitor、assign-delivery 的内存态演示。

## 验证记录

在 `frontend/` 下执行。最终验收复跑顺序为 `lint` -> `test:policies` -> `build`：

```bash
npm.cmd run test:policies
```

结果：通过。`81` tests，`81` pass，`0` fail。

```bash
npm.cmd run build
```

结果：通过。Vite build completed，保留既有 chunk size warning。

```bash
npm.cmd run lint
```

结果：通过。`eslint --fix` 执行完成，无剩余报错。

最终复跑记录：

- `npm.cmd run lint`：exit 0，无剩余报错。
- `npm.cmd run test:policies`：`81` tests，`81` pass，`0` fail。
- `npm.cmd run build`：exit 0，Vite build completed，保留既有 chunk size warning。

## 备注

- 未修改后端文件。
- 未实现 S5 预检、S6 灭火任务生成、S7 释放确认的真实联动，仅保留 UI 占位。
- 未执行 git commit。
