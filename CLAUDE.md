# uavfire — 项目指南

## 🚀 后端启动

- JDK 11 路径：`/usr/local/opt/openjdk@11`
- 启动命令：`JAVA_HOME=/usr/local/opt/openjdk@11 mvn spring-boot:run -pl uavfire`
- 端口：6789

## Karpathy 编码准则

以下准则衍生自 Andrej Karpathy 关于 LLM 编码陷阱的观察，用于减少常见编码错误。

### 1. 先思考，再动手

- 实现前先说出你的假设，不确定就问
- 如果存在多种解读方式，列出来，不要默默选一种
- 如果有更简单的方法，说出来
- 不清楚就停下来，描述困惑，然后问

### 2. 保持简单

- 只写解决问题所需的最小代码，不加推测性功能
- 不用为单次使用场景建抽象层
- 不要做没被要求的"灵活性"或"可配置性"
- 不给不可能发生的场景加错误处理
- 如果写了 200 行但可以缩成 50 行，重写

### 3. 精准修改

- 只动必须改的，只清理自己引入的混乱
- 不改相邻的无关代码、注释或格式
- 不重构没坏的东西
- 匹配现有风格
- 如果改动产生了遗留（未使用的 import/变量/函数），清理掉
- 不要删已有的死代码（除非被要求）

验证标准：每一行改动都应该能直接追溯到用户的需求。

### 4. 目标驱动

把任务转化成可验证的目标：
- "加验证" → "先写非法输入的测试，再让它们通过"
- "修 bug" → "先写复现 bug 的测试，再让它通过"
- "重构 X" → "确保重构前后测试都能通过"
